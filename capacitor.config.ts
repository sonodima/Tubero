import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.sonodima.tubero",
  appName: "tubero",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
